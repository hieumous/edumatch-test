services:
  # For CI smoke we replace frontend/chat with tiny stubs under the normal
  # docker-compose service names so nginx can resolve them consistently.

  chat-service:
    image: python:3.12-slim
    container_name: chat-service-ci-stub
    command: >
      python -c "import http.server, socketserver;
      class H(http.server.BaseHTTPRequestHandler):
        def do_GET(self):
          self.send_response(200); self.end_headers();
          self.wfile.write(b'ok')
        def log_message(self, format, *args):
          return
      socketserver.TCPServer(('0.0.0.0', 8083), H).serve_forever()"
    networks:
      - test-network

  frontend:
    image: python:3.12-slim
    container_name: frontend-ci-stub
    command: >
      python -c "import http.server, socketserver;
      class H(http.server.BaseHTTPRequestHandler):
        def do_GET(self):
          self.send_response(200); self.end_headers();
          self.wfile.write(b'frontend-stub')
        def log_message(self, format, *args):
          return
      socketserver.TCPServer(('0.0.0.0', 3000), H).serve_forever()"
    networks:
      - test-network
