# === Application Configuration ===
server.port=8083
spring.application.name=chat-service

# === Database Configuration ===
spring.datasource.url=jdbc:mysql://chat-db-test:3306/chat_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=@Saitamass2
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# === RabbitMQ Configuration ===
spring.rabbitmq.host=rabbitmq-test
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest
app.rabbitmq.exchange=events_exchange

# === Security Configuration ===
app.jwt.secret=EduMatch_Super_Secret_Key_!@#_DoNotShare_!@#
app.jwt.header=Authorization
app.jwt.prefix=Bearer

# === Firebase Configuration ===
app.firebase.sdk-path=classpath:firebase-adminsdk-key.json

# === Other Services URLs ===
app.services.auth-service.url=http://auth-service-test:8081

# === Logging ===
logging.level.org.springframework.security=DEBUG
logging.level.com.edumatch.chatservice=DEBUG
